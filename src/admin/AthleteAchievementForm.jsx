import React, { useEffect, useRef, useState } from 'react';

const field = 'mt-2 min-h-11 w-full rounded-md border border-asanda-line bg-white px-3 text-asanda-ink focus-visible:outline focus-visible:outline-2 focus-visible:outline-asanda-orange dark:border-slate-600 dark:bg-slate-800 dark:text-white';
const typeOptions = [['national_podium', 'Podio nacional'], ['international_podium', 'Podio internacional'], ['international_participation', 'Participación internacional'], ['state_record', 'Récord estatal']];
const newChild = (key, type) => ({ key, id: '', eventDefinitionId: '', eventName: '', eventActive: true, podiumPlace: ['national_podium', 'international_podium'].includes(type) ? '1' : '', participationOutcome: type === 'international_participation' ? 'top_8' : '', recordId: '', recordStatus: null, legacyEventLabel: '', legacyPayload: null, legacySourceIdentifier: '' });
const newGroup = (key = 'new-0') => ({ id: '', type: 'national_podium', title: '', competitionName: '', location: '', achievedOn: '', children: [newChild(key, 'national_podium')] });
const fromItem = (item) => item ? { ...item, children: item.children.length ? item.children.map((child, index) => ({ ...child, key: child.id || `saved-${index}` })) : [newChild('new-0', item.type)] } : newGroup();
const isLegacy = (child) => !child.eventDefinitionId && child.legacyEventLabel && child.legacyPayload && child.legacySourceIdentifier;
const recordAvailable = (record) => record?.publicationStatus === 'published' && record.publishedAt && Date.parse(record.publishedAt) <= Date.now();

const AthleteAchievementForm = ({ events = [], records = [], editing, busy, canCreate = true, onSubmit, onCancel }) => {
  const [values, setValues] = useState(() => newGroup()); const [focusKey, setFocusKey] = useState(null); const nextKey = useRef(0); const resultRefs = useRef({}); const addButton = useRef(null);
  useEffect(() => { nextKey.current = 0; setValues(fromItem(editing)); setFocusKey(null); }, [editing]);
  useEffect(() => { if (focusKey) { resultRefs.current[focusKey]?.focus(); setFocusKey(null); } }, [focusKey]);
  const update = (key) => (event) => setValues((current) => ({ ...current, [key]: event.target.value }));
  const updateType = (event) => setValues((current) => ({ ...current, type: event.target.value, children: current.children.map((child) => ({ ...child, podiumPlace: ['national_podium', 'international_podium'].includes(event.target.value) ? child.podiumPlace || '1' : '', participationOutcome: event.target.value === 'international_participation' ? child.participationOutcome || 'top_8' : '', recordId: event.target.value === 'state_record' ? child.recordId : '' })) }));
  const updateChild = (index, key) => (event) => setValues((current) => ({ ...current, children: current.children.map((child, childIndex) => childIndex !== index ? child : { ...child, [key]: event.target.value, ...(key === 'eventDefinitionId' && event.target.value ? { legacyEventLabel: '', legacyPayload: null, legacySourceIdentifier: '' } : {}) }) }));
  const addResult = () => { const key = `new-${++nextKey.current}`; setValues((current) => ({ ...current, children: [...current.children, newChild(key, current.type)] })); setFocusKey(key); };
  const removeResult = (index) => { const next = values.children[index - 1] || values.children[index + 1]; setValues((current) => ({ ...current, children: current.children.filter((_, childIndex) => childIndex !== index) })); if (next) setFocusKey(next.key); else addButton.current?.focus(); };
  const eventOptions = (child) => { const current = events.find((event) => event.id === child.eventDefinitionId); const stale = child.eventDefinitionId && (!current || child.eventActive === false) ? [{ id: child.eventDefinitionId, name: child.eventName || child.legacyEventLabel || 'Prueba no disponible', isActive: false }] : []; return [...stale, ...events.filter((event) => !stale.some((item) => item.id === event.id))]; };
  const recordOptions = (child) => { const current = records.find((record) => record.id === child.recordId); const stale = child.recordId && !current ? [{ id: child.recordId, eventName: 'Récord no disponible', competitionName: '', publicationStatus: null }] : []; return [...stale, ...records.filter((record) => !stale.some((item) => item.id === record.id))]; };
  const submit = async (event) => { event.preventDefault(); if (await onSubmit(values)) setValues(newGroup(`new-${++nextKey.current}`)); };
  return <form aria-label={editing ? `Editar competencia ${editing.title}` : 'Agregar competencia del atleta'} onSubmit={submit} className="mt-5 min-w-0 rounded-md border border-asanda-line bg-asanda-foam p-4 dark:border-slate-700 dark:bg-slate-900">
    <div className="grid min-w-0 gap-4 sm:grid-cols-2"><label className="text-sm font-bold">Tipo de logro<select className={field} value={values.type} onChange={updateType}>{typeOptions.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label className="text-sm font-bold">Título<input className={field} required maxLength="180" value={values.title} onChange={update('title')} placeholder="Ej. Podio nacional juvenil" /></label><label className="text-sm font-bold">Competencia<input className={field} required maxLength="180" value={values.competitionName} onChange={update('competitionName')} /></label><label className="text-sm font-bold">Ubicación<input className={field} required maxLength="180" value={values.location} onChange={update('location')} placeholder="Ej. Complejo de piscinas" /></label><label className="text-sm font-bold sm:col-span-2">Fecha de la competencia<input className={field} type="date" required value={values.achievedOn} onChange={update('achievedOn')} /></label></div>
    <div className="mt-5 space-y-3" aria-label="Resultados de la competencia">{values.children.map((child, index) => { const legacy = isLegacy(child); return <fieldset key={child.key} className="min-w-0 rounded-md border border-asanda-line bg-white p-4 dark:border-slate-700 dark:bg-slate-800"><legend className="px-2 font-bold">Resultado {index + 1}</legend><div className="grid min-w-0 gap-4 sm:grid-cols-2"><label className="text-sm font-bold sm:col-span-2">Prueba del resultado {index + 1}<select ref={(node) => { if (node) resultRefs.current[child.key] = node; }} className={field} required={!legacy} value={child.eventDefinitionId} onChange={updateChild(index, 'eventDefinitionId')}><option value="">{legacy ? 'Remediar: seleccioná una prueba' : 'Seleccioná una prueba'}</option>{eventOptions(child).map((item) => <option key={item.id} value={item.id}>{item.name}{item.isActive === false ? ' (no disponible)' : ''}</option>)}</select></label>{legacy && <p className="text-xs leading-5 text-amber-800 dark:text-amber-200 sm:col-span-2">Resultado legado pendiente de remediación: {child.legacyEventLabel}. Seleccioná una prueba oficial para habilitar la publicación.</p>}{['national_podium', 'international_podium'].includes(values.type) && <label className="text-sm font-bold">Posición del resultado {index + 1}<select className={field} value={child.podiumPlace} onChange={updateChild(index, 'podiumPlace')}><option value="1">1 - Primer lugar</option><option value="2">2 - Segundo lugar</option><option value="3">3 - Tercer lugar</option></select></label>}{values.type === 'international_participation' && <label className="text-sm font-bold">Resultado de participación {index + 1}<select className={field} value={child.participationOutcome} onChange={updateChild(index, 'participationOutcome')}><option value="top_8">Top 8</option><option value="outstanding_participation">Participación destacada</option></select></label>}{values.type === 'state_record' && <label className="text-sm font-bold sm:col-span-2">Récord estatal oficial {index + 1}<select className={field} required={!legacy} value={child.recordId} onChange={updateChild(index, 'recordId')}><option value="">Seleccioná un récord publicado</option>{recordOptions(child).map((item) => <option key={item.id} value={item.id}>{item.eventName}{item.competitionName ? ` · ${item.competitionName}` : ''}{recordAvailable(item) ? '' : ' (no disponible)'}</option>)}</select></label>}</div><div className="mt-3 flex flex-wrap items-center gap-3"><button type="button" disabled={values.children.length === 1 || busy} onClick={() => removeResult(index)} className="min-h-10 font-bold text-red-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-asanda-orange disabled:opacity-50 dark:text-red-300">Eliminar resultado</button>{child.eventDefinitionId && child.eventActive === false && <span className="text-xs font-semibold text-amber-800 dark:text-amber-200">La prueba guardada ya no está activa.</span>}{values.type === 'state_record' && child.recordId && child.recordStatus !== 'published' && <span className="text-xs font-semibold text-amber-800 dark:text-amber-200">El récord ya no está publicado.</span>}</div></fieldset>; })}</div>
    <div className="mt-4 flex flex-wrap items-center gap-3"><button ref={addButton} type="button" onClick={addResult} disabled={busy} className="min-h-11 rounded-md border border-asanda-deep px-4 font-bold text-asanda-deep focus-visible:outline focus-visible:outline-2 focus-visible:outline-asanda-orange dark:border-slate-400 dark:text-white">Agregar resultado</button>{!editing && !canCreate && <p role="note" className="text-sm font-semibold text-amber-800 dark:text-amber-200">El atleta ya tiene seis o más competencias. Remediá los datos existentes antes de crear otra.</p>}</div><div className="mt-4 flex flex-wrap gap-3"><button className="min-h-11 rounded-md bg-asanda-deep px-4 font-bold text-white focus-visible:outline focus-visible:outline-2 focus-visible:outline-asanda-orange disabled:opacity-60" disabled={busy || (!editing && !canCreate)} type="submit">{busy ? 'Guardando…' : editing ? 'Guardar cambios' : 'Agregar competencia'}</button>{editing && <button className="min-h-11 px-3 font-bold text-asanda-deep dark:text-white" type="button" onClick={onCancel}>Cancelar edición</button>}</div>
  </form>;
};

export default AthleteAchievementForm;
